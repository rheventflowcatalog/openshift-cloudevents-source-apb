version: 1.0
name: openshift-cloud-event-monitor-apb
description: Monitor the openshift platform and emit Cloud Events
bindable: False
async: optional
metadata:
  displayName: Openshift Cloud Event Monitor
  imageUrl: "https://avatars0.githubusercontent.com/u/792337?s=200&v=4"
plans:
  - name: default
    description: This deploys the Openshift CloudEvents Monitor
    free: True
    metadata: {}
    parameters:
    - name: admin_user
      title: Openshift Admin User
      type: string
      required: true
    - name: admin_password
      title: Openshift Admin Password
      type: string
      display_type: password
      required: true
