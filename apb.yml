version: 1.0
name: openshift-cloud-event-source-apb
description: This is a sample application generated by apb init
bindable: False
async: optional
metadata:
  displayName: CloudEvent Source
  imageUrl: "https://raw.githubusercontent.com/project-stremzi/env-var-apb/master/icon.svg?sanitize=true"
plans:
  - name: default
    description: This default plan deploys openshift-cloud-event-source-apb
    free: True
    metadata: {}
    parameters:
    - name: admin_user
      title: Openshift Admin User
      type: string
      required: true
    - name: admin_password
      title: Openshift Admin Password
      type: string
      display_type: password
      required: true
